You are the IMPLEMENTATION agent for Conan's Android game factory. Target: "Gravity Flux" (slug: gravity-flux), an Android puzzle where one tap rotates gravity 90° and a flux orb slides until blocked; route it through all NODEs into the EXIT portal.

Working directory is the project root (projects/gravity-flux). The Gradle/Kotlin (View stack, NOT Compose) scaffold already exists (settings.gradle.kts, build.gradle.kts, app/build.gradle.kts, gradle wrapper, gradle.properties). docs/spec.md, docs/technical-plan.md, docs/tasks.md are complete — follow them.

CRITICAL ENVIRONMENT SETUP (do this first):
- Create file `local.properties` at the project root with exactly one line:
  sdk.dir=/home/openclaw/.openclaw/workspace/.local/android-sdk
  (This points the build at the already-installed Android SDK. Do NOT change this path.)

Create ALL of the following files with complete, compiling, production-quality code:

1. app/src/main/AndroidManifest.xml — single MainActivity, portrait, appropriate theme.
2. app/src/main/kotlin/com/iamconanpeter/gravityflux/engine/GravityFluxEngine.kt — PURE Kotlin, zero Android deps. Must include:
   - enum Cell { EMPTY, WALL, NODE, EXIT, HAZARD, START }
   - enum Gravity { DOWN, LEFT, UP, RIGHT } with clockwise rotate()
   - data class Chamber(grid: List<List<Cell>>, startRow, startCol, par)
   - enum GameState { PLAYING, WON, FAILED }
   - class GravityFluxEngine(chamber): properties orbRow, orbCol, gravity, collectedNodes(Set), moves, state, undoAvailable
   - rotateAndSlide(): rotate gravity clockwise then slideUntilBlocked (snapshot pushed for undo before mutation), increment moves
   - slide direction handling: step one cell at a time; stop at WALL/out-of-bounds (stay in last valid cell); enter HAZARD => state=FAILED; collect NODE along path; if reach EXIT and all nodes collected => state=WON
   - undo(): single token, restore orb/gravity/collected/moves/state
   - reset(): restore initial chamber state
   - stars(): 3 if moves<=par and undo unused; 2 if moves<=par+2; else 1
   - companion: campaign chambers list (at least 6, growing 5x5 to 9x9), dailyIndex(dayOfYear:Int) = dayOfYear % size, and validateSolvable helper (BFS over gravity-rotation state space) so every campaign chamber is provably solvable.
3. app/src/main/kotlin/com/iamconanpeter/gravityflux/data/FluxProgressManager.kt — SharedPreferences best-star per level + streak-safe daily solve counter + daily index storage.
4. app/src/main/kotlin/com/iamconanpeter/gravityflux/audio/FluxAudioManager.kt — SoundPool wrapper with synthesized short PCM blips (whoosh/chime/hum/thud) generated in code (no asset files), mute support.
5. app/src/main/kotlin/com/iamconanpeter/gravityflux/ui/GravityFluxView.kt — custom Canvas View: grid, neon orb glow, pulsing nodes, flickering hazards, swirling portal, slide tween via ValueAnimator, no per-frame allocation.
6. app/src/main/kotlin/com/iamconanpeter/gravityflux/ui/MainActivity.kt — AppCompatActivity hosting the view + buttons (Bend Gravity, Undo, Reset, Next), HUD (moves, par, stars, daily/streak).
7. app/src/main/res/values/strings.xml, colors.xml, themes.xml, and any layout needed.
8. app/src/test/kotlin/com/iamconanpeter/gravityflux/GravityFluxEngineTest.kt — JUnit4 tests: slide-until-blocked, wall stop, out-of-bounds stop, node collect along path, hazard fail, win condition (exit+all nodes), undo restore, reset, dailyIndex determinism, star rating boundaries, and a test asserting ALL campaign chambers are solvable.

Requirements: Kotlin source under src/main/kotlin and src/test/kotlin (the kotlin("android") plugin already includes src/main/kotlin as a source dir; do not add custom sourceSets unless needed). minSdk 21, Java 17. Deterministic, no RNG in physics. Keep code clean and well-commented.

After writing all source:
- Run `./gradlew test assembleDebug` (use the local gradlew). Iterate to fix ANY compile errors or test failures until the command exits successfully (BUILD SUCCESSFUL and tests green).
- Write `README.md` at the project root documenting: what the game is, how to build/run (./gradlew test assembleDebug), controls, and a short "Built with Codex CLI" note citing the model used (qwen/qwen3-coder:free via OpenRouter) as evidence.

STRICT BOUNDARIES:
- Do NOT run any git commands (commit/push/remote) — that is handled separately.
- Do NOT run network/install commands (no pip/npm/brew). The SDK and gradle deps are already present.
- Keep scope to the MVP above (per docs/tasks.md). Do not add ad SDKs, IAP, or backend.
- When done, report a concise summary of files created and the final gradle result.
