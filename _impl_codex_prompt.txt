You are the IMPLEMENTATION agent for Conan's Android game factory. Target: "Gravity Flux" (slug: gravity-flux), an Android puzzle where one tap rotates gravity 90° and a flux orb slides until blocked; route it through all NODEs into the EXIT portal.

Working directory is the project root (projects/gravity-flux). The Gradle/Kotlin (View stack, NOT Compose) scaffold already exists (settings.gradle.kts, build.gradle.kts, app/build.gradle.kts, gradle wrapper, gradle.properties, local.properties). docs/spec.md, docs/technical-plan.md, docs/tasks.md are complete — follow them.

CRITICAL ENVIRONMENT NOTE:
- local.properties already exists and points sdk.dir at the installed Android SDK. Do NOT change it.
- The build is validated SEPARATELY by another process. DO NOT run gradle, ./gradlew, or any build/install/network commands yourself. Just WRITE the source files.

Create ALL of the following files with complete, compiling, production-quality Kotlin (and XML) code. Every file must be fully implemented — no TODOs, no stubs, no "implement later".

1. app/src/main/AndroidManifest.xml — single MainActivity, portrait, no action bar theme (Theme.MaterialComponents.DayNight.NoActionBar or AppCompat equivalent), INTERNET not required.

2. app/src/main/kotlin/com/iamconanpeter/gravityflux/engine/GravityFluxEngine.kt — PURE Kotlin, ZERO Android imports. Must include:
   - enum class Cell { EMPTY, WALL, NODE, EXIT, HAZARD, START }
   - enum class Gravity { DOWN, LEFT, UP, RIGHT } with fun rotate(): rotates clockwise (DOWN->LEFT->UP->RIGHT->DOWN)
   - data class Chamber(val grid: List<List<Cell>>, val startRow: Int, val startCol: Int, val par: Int)
   - enum class GameState { PLAYING, WON, FAILED }
   - class GravityFluxEngine(private val chamber: Chamber) with properties: orbRow, orbCol, gravity (start DOWN), collectedNodes: MutableSet<Pair<Int,Int>>, moves: Int, state: GameState, undoAvailable: Boolean
   - rotateAndSlide(): push snapshot for undo FIRST, then rotate gravity clockwise, then slideUntilBlocked(); increment moves; do NOT allow moves after WON/FAILED
   - slideUntilBlocked(): from orb cell, repeatedly step one cell in current gravity direction:
       * if next cell is out-of-bounds or WALL -> stop in current cell
       * if next cell is HAZARD -> enter it, set state = FAILED, stop
       * else move into next cell; if it is NODE add to collectedNodes; if it is EXIT and collectedNodes.size == totalNodes -> state = WON, stop; continue sliding
   - fun totalNodes(): count NODE cells in chamber
   - undo(): if undoAvailable, restore last snapshot (orb/gravity/collected/moves/state), set undoAvailable = false
   - reset(): restore to initial chamber state (orb at START, gravity DOWN, empty collected, moves 0, state PLAYING, undoAvailable true)
   - stars(): 3 if moves <= par && undoAvailable; 2 if moves <= par + 2; else 1  (note: undoAvailable true means undo was not used)
   - companion object: val CAMPAIGN: List<Chamber> (>= 6 chambers, grid sizes growing 5x5 -> 9x9, each with START, >=1 NODE, exactly 1 EXIT, some WALLS, a few HAZARDs; every chamber MUST be solvable), fun dailyIndex(dayOfYear: Int): Int = ((dayOfYear % CAMPAIGN.size) + CAMPAIGN.size) % CAMPAIGN.size, and fun isSolvable(chamber: Chamber): Boolean (BFS over states (orbRow,orbCol,gravity) exploring all 4 rotations until WON or exhausted; cap visited states to avoid infinite loops)
   - var totalNodesCount cached from chamber at construction

3. app/src/main/kotlin/com/iamconanpeter/gravityflux/data/FluxProgressManager.kt — wraps SharedPreferences (context.getSharedPreferences("gravity_flux", Context.MODE_PRIVATE)):
   - getBestStars(level: Int): Int and setBestStars(level: Int, stars: Int) (only overwrites if better)
   - getDailySolved(dateKey: String): Boolean / markDailySolved(dateKey: String)
   - getStreak(): Int and bumpStreak() (streak-safe: if yesterday solved, increment; if today already solved, keep; if gap, reset to 1) — keep logic simple and correct
   - lastDailyDate tracking
   Use android.content.Context and android.content.SharedPreferences imports.

4. app/src/main/kotlin/com/iamconanpeter/gravityflux/audio/FluxAudioManager.kt — SoundPool wrapper:
   - Generate short PCM sine/triangle blips in code (no asset files) for: whoosh (rotate), chime (collect), hum (win), thud (hazard). Build AudioTrack or SoundPool with generated byte arrays.
   - fun play(event: FluxEvent) where FluxEvent is an enum { ROTATE, COLLECT, WIN, HAZARD }
   - mute support: fun setMuted(Boolean), fun isMuted()
   - proper release() on destroy
   Use android.media.* and android.content.Context.

5. app/src/main/kotlin/com/iamconanpeter/gravityflux/ui/GravityFluxView.kt — custom View (extends View):
   - Renders the chamber grid, neon orb with radial glow (RadialGradient), pulsing NODEs, flickering HAZARDs, swirling EXIT portal, WALLs, START marker.
   - Slide animation via ValueAnimator interpolating orb position from old cell to new cell (no per-frame allocations: precompute paint objects in init).
   - public fun setEngine(engine: GravityFluxEngine) and fun render() to redraw.
   - public fun animateSlide(fromRow,fromCol,toRow,toCol, onEnd: () -> Unit)
   - handle density (dp) scaling.
   Use android.graphics.*, android.view.View, android.animation.ValueAnimator, android.content.Context.

6. app/src/main/kotlin/com/iamconanpeter/gravityflux/ui/MainActivity.kt — AppCompatActivity:
   - Layout: GravityFluxView + HUD (moves, par, stars, daily/streak TextViews) + buttons (Bend Gravity, Undo, Reset, Next).
   - On "Bend Gravity": call engine.rotateAndSlide(); animate slide; update HUD; play sound; on WON show win overlay + stars + update FluxProgressManager; on FAILED show fail overlay.
   - "Undo"/"Reset" wired to engine.undo()/reset() with re-render.
   - "Next" loads next campaign chamber (or daily chamber on launch).
   - On launch: select daily chamber via GravityFluxEngine.dailyIndex(Calendar.getInstance().get(Calendar.DAY_OF_YEAR)) and show streak from FluxProgressManager.
   - Use Material/components or simple LinearLayout/FrameLayout with buttons; programmatic layout is fine.
   - Wire FluxAudioManager events.

7. app/src/main/res/values/strings.xml, colors.xml, themes.xml (and styles if needed). Provide a NoActionBar theme.

8. app/src/test/kotlin/com/iamconanpeter/gravityflux/GravityFluxEngineTest.kt — JUnit4 (import org.junit.*, androidx.test? NO — pure JVM JUnit4, no android deps). Tests MUST cover:
   - slide-until-blocked stops at wall
   - out-of-bounds stop
   - node collected along path
   - hazard causes FAILED
   - win condition when EXIT reached with all nodes collected
   - undo restores previous state
   - reset restores initial state
   - dailyIndex determinism (same input -> same output, within range)
   - star rating boundaries (moves <= par, par+1, par+2, par+3)
   - assert GravityFluxEngine.CAMPAIGN.all { GravityFluxEngine.isSolvable(it) } is true (every campaign chamber solvable)
   Build small test chambers via Chamber(...) directly in the test.

Requirements:
- Kotlin source under src/main/kotlin and src/test/kotlin. minSdk 21, Java 17. Deterministic, NO RNG in physics. Clean, well-commented code.
- The engine file must NOT import any android.* classes (pure JVM so JUnit4 can test it).
- Make sure package names match: com.iamconanpeter.gravityflux(.engine/.data/.audio/.ui).

When done, report a concise list of files you created and any assumptions. Do NOT run gradle.
