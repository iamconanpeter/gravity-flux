<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No-action-bar theme for the whole app (AppCompat + Material). -->
    <style name="Theme.GravityFlux.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/neon_cyan</item>
        <item name="colorPrimaryDark">@color/bg_deep</item>
        <item name="colorAccent">@color/neon_purple</item>
        <item name="android:windowBackground">@color/bg_deep</item>
        <item name="android:statusBarColor">@color/bg_deep</item>
    </style>
</resources>
